---
- name: Debian - Disable IPv6
  hosts: all
  remote_user: sysops
  gather_facts: false
  become: yes

  tasks:
  - name: List Debian version
    shell: cat /etc/debian_version
    register: shell_output

  - debug:
      var: shell_output.stdout

#  - name: Dump all vars
#    action: template src=/etc/ansible/templates/dumpall.j2 dest=/etc/ansible/reports/uptime.txt
